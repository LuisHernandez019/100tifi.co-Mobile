import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '100tifi.co-Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
